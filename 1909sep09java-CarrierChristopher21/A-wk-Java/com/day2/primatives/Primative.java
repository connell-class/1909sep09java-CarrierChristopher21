package com.day2.primatives;

public class Primative {

	/*
	 * A bit is a 0 or a 1 (a 'on' or a 'off') A byte is 8 bits A nibble is 4 bits
	 * 
	 * meter to kilometer= 1000 meters byte to a kilobyte= 1024 2^10=1024
	 * 
	 * char, long, short, boolean, int, float, double, byte boolean (1/8 bytes) or 1
	 * bit byte(1 byte) or 8 bits char(2 bytes) or 16 bits short(2 bytes) or 16 bits
	 * float(4 bytes) or 32 bits int(4 bytes) or 32 bits long(8 bytes) or 64 bits
	 * double(8 bytes) or 64 bits
	 * 
	 * what is a variable? container for data, a placeholder for a value, etc.
	 * 
	 * how do we declare a variable in java?
	 * 
	 * 
	 */
	public static void main(String[] args) {
		// this is a variable initialization
		int x = 16;
		// this is a variable declaration
		double d;
		float f;
//		a quick comment with cntrol /
		System.out.println(x);
//sysout then cntrol space
		boolean b = true;
	}

}
